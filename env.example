############################################
# MEMG example .env (minimal)
############################################

# Embeddings (optional)
EMBEDDER_MODEL=Snowflake/snowflake-arctic-embed-xs

# REQUIRED: Port must be set explicitly for multi-instance support
# Use different ports for parallel testing: 8787, 8788, 8789, etc.
BASE_MEMORY_PATH=$HOME/.local/share/memory_system
MEMORY_SYSTEM_MCP_PORT=8787  # CHANGE THIS for each instance!

# Host binding (Docker sets this automatically to 0.0.0.0, localhost for development)
# MEMORY_SYSTEM_MCP_HOST=127.0.0.1  # Uncomment for localhost-only access
MEMG_TEMPLATE=software_development

# Storage paths handled by Docker Compose
MEMORY_SYSTEM_DEBUG=false

# Core runtime
QDRANT_COLLECTION=memories
EMBEDDING_DIMENSION_LEN=384
