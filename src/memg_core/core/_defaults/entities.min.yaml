version: 1
entities:
  - name: note
    anchor: statement
    fields:
      required: [statement]
      optional: [title, tags]
  - name: document
    anchor: statement
    fields:
      required: [statement]
      optional: [title, details, summary, tags]
  - name: task
    anchor: statement
    fields:
      required: [statement]
      optional: [title, due_date, status, tags]
